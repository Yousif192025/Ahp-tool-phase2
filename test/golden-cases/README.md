# Golden Test Cases

Populated at the start of Phase 3. Each file will record a representative
set of inputs (goal, criteria, alternatives, pairwise matrices) alongside
the exact numeric outputs (priority vectors, consistency ratios, final
scores) produced by the current, unmodified `index.html`. These become
the regression fixtures the extracted AHP engine and AMSESHI framework
must reproduce exactly.
